// PLACEHOLDER — overwrite by running `flutterfire configure` inside mobile_app/
// (use the same Firebase project as the web portal).
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform =>
      throw UnsupportedError('Firebase is not configured. Run `flutterfire configure` in mobile_app/.');
}
